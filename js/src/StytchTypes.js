// Typed models for the Stytch SDK (JSDoc typedefs).
//
// GENERATED from the API model: main.kit.entity.<e>.fields[] and per-op
// params (op.<name>.points[].args.params[]). Field/param types come from the
// canonical type sentinels via @voxgig/sdkgen canonToType (source of truth:
// @voxgig/apidef VALID_CANON). Annotations only — no runtime effect. Do not
// edit by hand.

/**
 * @typedef {Object} User
 * @property {string} [created_at]
 * @property {Array} [emails]
 * @property {Object} [name]
 * @property {string} [status]
 * @property {Object} [trusted_metadata]
 * @property {string} [user_id]
 */

/**
 * @typedef {Object} UserLoadMatch
 * @property {string} id
 */

/**
 * @typedef {Object} UserListMatch
 * @property {string} [created_at]
 * @property {Array} [emails]
 * @property {Object} [name]
 * @property {string} [status]
 * @property {Object} [trusted_metadata]
 * @property {string} [user_id]
 */

/**
 * @typedef {Object} UserCreateData
 * @property {string} [created_at]
 * @property {Array} [emails]
 * @property {Object} [name]
 * @property {string} [status]
 * @property {Object} [trusted_metadata]
 * @property {string} [user_id]
 */

/**
 * @typedef {Object} UserUpdateData
 * @property {string} id
 * @property {string} [created_at]
 * @property {Array} [emails]
 * @property {Object} [name]
 * @property {string} [status]
 * @property {Object} [trusted_metadata]
 * @property {string} [user_id]
 */

/**
 * @typedef {Object} UserRemoveMatch
 * @property {string} id
 */

